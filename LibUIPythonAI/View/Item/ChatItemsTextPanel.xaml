<UserControl
    x:Class="LibUIPythonAI.View.Item.ChatItemsTextPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:commonModel="clr-namespace:LibUIPythonAI.Resource"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:editorControl="clr-namespace:LibUIPythonAI.Control.Editor"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:local="clr-namespace:LibUIPythonAI.View.Item"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:res="clr-namespace:LibUIPythonAI.View.Item"
    xmlns:viewModel="clr-namespace:LibUIPythonAI.ViewModel.Item"
    d:DataContext="{d:DesignInstance Type=viewModel:ContentItemViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/LibUIPythonAI;component/Resource/Dictionary1.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <editorControl:MyTextBox
            x:Name="Editor"
            Grid.Row="0"
            Margin="10,3,10,3"
            FontSize="10pt"
            IsReadOnly="True"
            Text="{Binding ChatItemsText, Mode=OneWay}"
            TextWrappingMode="{Binding TextWrappingMode}"
            Visibility="{Binding TextVisibility}">
            <editorControl:MyTextBox.ContextMenu>
                <ContextMenu>
                    <MenuItem Command="ApplicationCommands.Copy" />
                    <MenuItem Command="ApplicationCommands.SelectAll" />
                    <Separator />
                </ContextMenu>
            </editorControl:MyTextBox.ContextMenu>
        </editorControl:MyTextBox>
        <editorControl:BindableRichTextBox
            Grid.Row="0"
            Margin="10,10,10,10"
            VerticalContentAlignment="Top"
            Document="{Binding MarkdownChatItemsText, Mode=OneWay}"
            FontSize="12"
            HorizontalScrollBarVisibility="Auto"
            IsReadOnly="True"
            VerticalScrollBarVisibility="Auto"
            Visibility="{Binding MarkdownVisibility}" />

    </Grid>
</UserControl>
