<Window x:Class="WpfApp1.View.ClipboardItemView.EditItemWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1.View.ClipboardItemView"
        mc:Ignorable="d"
        Title="新規作成" Height="450" Width="800">
    <Window.DataContext>
        <local:EditItemWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Label Content="説明" Grid.Row="0" Grid.Column="0"/>
        <TextBox Text="{Binding Description}" Grid.Row="0" Grid.Column="1"
                 Width="700" Margin="0,10,0,10"
                 IsEnabled="True"
                 />
        <Label Content="内容" Grid.Row="1" Grid.Column="0"/>
        <TextBox Text="{Binding Content}" Grid.Row="1" Grid.Column="1" 
                 Width="700" Height="300" Margin="0,10,0,10"
                 IsEnabled="True"
                 AcceptsReturn="True"
                  />
        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal">
            <Button Content="OK" Command="{Binding OKButtonCommand}" Margin="10,10,10,10"/>
            <Button Content="キャンセル" Command="{Binding CancelButtonCommand}" Margin="10,10,10,10"/>
        </StackPanel>
    </Grid>
</Window>
