<Window x:Class="ClipboardApp.View.TagView.TagWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ClipboardApp.View.TagView"
        mc:Ignorable="d"
        Title="タグ編集" Height="300" Width="500">
    <Window.DataContext>
        <local:TagWindowViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ListBox Name="listbox1" ItemsSource="{Binding TagList}" Grid.Row="0"
                 HorizontalAlignment="Stretch" 
                 Margin="10,10,10,10" 
                 VerticalAlignment="Stretch"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 >
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal"
                     >
                        <CheckBox Content="{Binding Tag}" IsChecked="{Binding IsChecked}" Width="200"/>
                        <StackPanel.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="削除" 
                                    Command="{Binding DeleteTagCommand}"
                                    CommandParameter="{Binding}"/>
                            </ContextMenu>
                        </StackPanel.ContextMenu>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Grid  Grid.Row="1" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <DockPanel HorizontalAlignment="Stretch" Grid.Column="0">
                <TextBlock Text="新規タグ" Margin="10,0,10,0" 
                           Style="{StaticResource MaterialDesignBody1TextBlock}"
                           />
                <TextBox Name="TagText" Margin="10,0,10,0"  
                         Text="{Binding NewTag}"
                         />
            </DockPanel>
            <Button Content="追加" Width="100" HorizontalAlignment="Right" Grid.Column="1"
                        Command="{Binding AddTagCommand}"  
                        CommandParameter="{Binding NewTag}" Margin="10,0,10,0" />
        </Grid>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" 
                        Command="{Binding OkCommand}" 
                        CommandParameter="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Window}}"
                        Margin="10"  Width="100"/>
            <Button Content="キャンセル"
                        Command="{Binding CancelCommand}"
                        CommandParameter="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Window}}"
                        Margin="10"  Width="100"/>
        </StackPanel>

    </Grid>
</Window>
